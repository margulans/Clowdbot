# 🧠 Задачи для системы рефлексий

## Cron задачи

### Ежедневная рефлексия (20:30 местного времени)

```json
{
  "schedule": { "kind": "cron", "expr": "30 20 * * *", "tz": "Asia/Dubai" },
  "payload": { "kind": "systemEvent", "text": "daily_reflection()" }
}
```

### Еженедельная аналитика (воскресенье 20:30 местного времени)

```json
{
  "schedule": { "kind": "cron", "expr": "30 20 * * 0", "tz": "Asia/Dubai" },
  "payload": { "kind": "systemEvent", "text": "weekly_reflection_analytics()" }
}
```

### Утренний план (06:00 местного времени)

```json
{
  "schedule": { "kind": "cron", "expr": "0 6 * * *", "tz": "Asia/Dubai" },
  "payload": { "kind": "systemEvent", "text": "morning_action_plan()" }
}
```

## Функции для реализации

### `daily_reflection()`

1. Провести мою собственную рефлексию
2. Задать 3 вопроса пользователю
3. Записать результаты в `data/reflections-YYYY-MM.json`
4. Обновить статистику

### `weekly_reflection_analytics()`

1. Проанализировать рефлексии недели
2. Выявить паттерны и тренды
3. Создать инсайты и рекомендации
4. Отправить отчет пользователю

### `morning_action_plan()`

**Цель:** сформировать краткий, конкретный план дня на основе контекста — не общие советы, а конкретные задачи.

#### Шаг 1. Сбор контекста

```
1. Определить дату вчера (YYYY-MM-DD)
2. read_file("data/reflections-YYYY-MM.json")
   → извлечь запись за вчера:
     - user_reflection.improve  → что Маргулан хотел изменить
     - tomorrow_intention       → намерение на сегодня (если заполнено)
     - metrics.energy           → вчерашняя энергия
3. read_file("USER.md")
   → извлечь:
     - ## Текущие цели          → квартальные приоритеты
     - ## Активные проекты      → что сейчас в работе
```

#### Шаг 2. Формирование плана

На основе данных определить:

**MVP дня (1 задача):**

- Если есть `tomorrow_intention` → это и есть MVP дня
- Если нет → выбери задачу из активных проектов с ближайшим дедлайном или наибольшим приоритетом

**Поддерживающие задачи (2–3 задачи):**

- Из `user_reflection.improve` — что конкретно сделать иначе сегодня
- Из активных проектов — следующий шаг по каждому

**Напоминание:**

- Если вчерашняя `metrics.energy` ≤ 5 → добавить: «⚡ Вчера было мало сил — начни с малого, набери темп»
- Если `metrics.energy` ≥ 8 → добавить: «🚀 Вчера высокая энергия — хорошее время для сложной задачи»

#### Шаг 3. Отправка брифинга

Отправить в **личку** Маргулану. Формат — максимально краткий, ≤ 8 строк:

```
🌅 Доброе утро, Маргулан!

🎯 MVP дня: [одна главная задача]

📋 Также:
• [задача 2]
• [задача 3]

[напоминание из вчерашней рефлексии — если есть]
[примечание по энергии — если нужно]
```

**Правила:**

- Никаких мотивационных фраз и воды
- Только конкретные задачи из контекста
- Если нет данных за вчера — не фантазировать, написать: «Нет рефлексии за вчера — напомню цели из плана» и показать 1-2 цели из USER.md
- Если USER.md не заполнен (цели — placeholder) — попросить Маргулана заполнить цели
