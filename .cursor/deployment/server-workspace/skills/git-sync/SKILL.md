---
name: git-sync
description: Коммит и пуш в Clowdbot
user-invocable: true
---

# Git Sync

Workspace бота связан с Git-репо через симлинк:
`~/.openclaw/workspace` → `~/Clowdbot/.cursor/deployment/server-workspace`

Все изменения в workspace автоматически попадают в Git-репо.

## Когда выполнять (автоматически, без команды /git):

Выполняй этот скилл сразу после ЛЮБОГО из событий:

- ✅ Создал, изменил или удалил cron-задачу через `cron` tool
- ✅ Завершил задачу пользователя, которая изменила конфиг/данные
- ✅ Обновил промпт / расписание / настройки через API
- ✅ Пользователь сказал `/git` или "сохрани", "запушь", "зафикси"

**Не жди команды — это твоя ответственность.**

## При /git:

1. `cd ~/Clowdbot`
2. `git pull origin main` (получить последние изменения)
3. **Обнови снапшот cron-задач** (см. раздел ниже)
4. `git status --short`
5. Если нет изменений → "✅ Working tree чистое". Стоп.
6. `git add -A`
7. `git commit -m "<тип>: <описание>"` (сгенерируй сам)
8. `git push origin main`

## Эволюция Нейрона (перед коммитом):

Перед каждым коммитом — один вопрос:

> **Добавляет ли это изменение новую ценность Нейрону?**  
> — новый скилл / новый cron job / новая возможность / снятая с Маргулана задача

**Да** → обнови `.ai/EVOLUTION.md` (путь: `~/Clowdbot/.ai/EVOLUTION.md`):

1. Найти нужную категорию (1–5) или создать новую
2. Добавить в «Что делает» и «Что снял с меня»
3. Добавить строку в хронологический журнал внизу файла

**Нет** (мелкий фикс, обновление данных, конфиг без нового поведения) → пропустить.

---

## Снапшот cron-задач (обязательно перед коммитом):

Cron-задачи хранятся в БД OpenClaw и **не попадают в git автоматически**.
Перед каждым `/git` — проверь, были ли изменения в cron-задачах с последнего коммита.

**Если были изменения** → обнови `data/cron-jobs-snapshot.json`:

1. Запроси актуальный список через `cron(action=list)`
2. Обнови файл: имя, расписание, wakeMode, model, краткое описание промпта
3. Добавь `"last_fixed": "YYYY-MM-DD"` у изменённых задач

**Если изменений не было** → пропусти этот шаг.

## Вывод:

- 🌿 Ветка: `main`
- 📝 Commit: SHA
- 📊 Файлы: список

## Типы коммитов:

feat/fix/docs/refactor/config/update
