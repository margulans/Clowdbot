---
name: doctor
description: Автономная система мониторинга и самолечения Нейрона. Проверяет выполнение крон-задач, здоровье системы и зависимостей. Чинит сбои, запускает бэкапы и шлет отчеты.
command-description: Запустить диагностику и ремонт системы
user-invocable: yes
---

# Доктор 🩺 (Система Самолечения)

> «Здоровая система — это не та, что не падает, а та, что сама встает и делает выводы.»

Скилл «Доктор» — это высший контур управления Нейроном. Он следит за тем, чтобы каждая автоматизация (cron) не просто «отработала», а принесла качественный результат.

## Основной алгоритм (Цикл Доктора)

### 1. Проверка пульса (Диагностика)
- **Cron Health:** Выполни `cron(action=list)`. Найди задачи с `lastStatus == "error"` или `consecutiveErrors > 0`.
- **System Health:** Выполни `openclaw status`. Проверь работу каналов (Telegram).
- **Dependency Health:** Проверь наличие и версии критических бинарников: `yt-dlp`, `summarize`, `git`, `python3`.
- **Git Health:** Проверь `git status`. Если есть «грязные» файлы более 4 часов — это аномалия.

### 2. Анализ и Бэкап
Если обнаружен сбой в крон-задаче:
1. **Мгновенный бэкап:** Если задача пропущена (например, утренний дайджест), Доктор ОБЯЗАН немедленно запустить соответствующую Backup-задачу через `cron(action=run)`.
2. **Investigation:** Выясни корневую причину. Проверь:
    - Модель (не возвращает ли 0 токенов?).
    - API ключи (не протухли ли?).
    - Сетевые блокировки (как с YouTube).
    - Ошибки конфигурации (wakeMode, sessionTarget).

### 3. Ремонт (Self-Healing)
- **Fix:** Исправь причину (обнови конфиг, переключи модель на fallback, очисти кэш).
- **Restart:** Если проблема в зависшем шлюзе — выполни `gateway(action=restart)`.
- **Test:** Запусти исправленную задачу в тестовом режиме (`runMode: "force"`).

### 4. Отчетность
После каждого вмешательства Доктор шлет Маргулану краткий отчет:
```
🩺 **ОТЧЕТ ДОКТОРА**
─────────────────────
🔴 **Сбой:** [Название задачи]
🔍 **Причина:** [Техническое описание]
🔧 **Исправлено:** [Что было сделано]
🧪 **Тест:** [Результат проверки]
📊 **Статус системы:** Стабильна
```

## Режимы запуска

1. **Реактивный (интегрированный):** Добавляется как обязательный финальный шаг в важные cron-задачи.
2. **Проактивный (Watcher):** Запускается по собственному расписанию (каждые 30 минут) для проверки истории логов.

---
🛡️ **ОБЯЗАТЕЛЬНЫЙ АУДИТ (в конце):**
> **🛡️ Аудит Доктора:**
> - Проверено систем: [список]
> - Выявлено сбоев: [число]
> - Статус самолечения: [выполнено/не требуется] ✅
