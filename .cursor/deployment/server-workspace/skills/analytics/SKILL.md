---
name: analytics
description: Аналитик — еженедельный анализ паттернов рефлексий. Выявляет тренды энергии, настроения и фокуса, находит системные blockers, отслеживает прогресс по целям. Отправляет короткий инсайт-отчёт.
command-description: Анализировать паттерны рефлексий за неделю
user-invocable: yes
metadata: { "openclaw": { "emoji": "📊" } }
---

# Аналитик 📊

> «Данные говорят. Я перевожу.»

Читает рефлексии за последние 7 дней, вычисляет средние метрики, ищет паттерны, сравнивает с целями из USER.md. Не интерпретирует субъективно — только факты + выводы из данных.

## Запуск

- **Автоматически:** каждое воскресенье в 21:00 Алматы — режим `weekly` (после вечерней рефлексии)
- **Вручную (команда):** `/analytics` или `/аналитика` — анализ за последние 7 дней
- **Вручную с периодом:** `/analytics 30` или `/аналитика 30` — анализ за N дней

Маршрутизация: описание намерений — в `AGENTS.md` (раздел «Маршрутизация скиллов»).

---

## РЕЖИМ: WEEKLY

### Шаг 1. Загрузка данных

```
1. read_file("USER.md")                          → цели, проекты, жизненный контекст
2. read_file("data/reflections-YYYY-MM.json")    → текущий месяц
3. [если неделя пересекает два месяца]
   read_file("data/reflections-YYYY-MM.json")    → предыдущий месяц
```

Извлечь записи за последние 7 дней (включая сегодня).

Если за период меньше 3 записей — сообщи Маргулану:

> «📊 Аналитик: за неделю только N рефлексий — недостаточно для надёжных выводов. Постараюсь найти паттерны из того что есть.»
> И продолжай с имеющимися данными.

---

### Шаг 2. Вычисление метрик

Для каждой найденной записи извлечь поля `metrics` (если есть).

Вычислить средние по неделе:

- `avg_energy` = среднее всех `metrics.energy`
- `avg_mood` = среднее всех `metrics.mood`
- `avg_focus` = среднее всех `metrics.focus`

Вычислить тренд (сравнить первую половину недели со второй):

- Если avg второй половины > avg первой на 0.5+ → `trend = "растёт ↑"`
- Если avg второй половины < avg первой на 0.5+ → `trend = "падает ↓"`
- Иначе → `trend = "стабильно →"`

Найти лучший день (максимальная сумма energy+mood+focus) и худший день.

---

### Шаг 3. Анализ wins и blockers

Собрать все записи `wins` из рефлексий за неделю в один список.
Собрать все записи `blockers` из рефлексий за неделю в один список.

Найти повторяющиеся темы (если одно и то же встречается 2+ раза — это паттерн):

- Повторяющийся win → «это работает, делай больше»
- Повторяющийся blocker → «системная проблема, требует решения»

---

### Шаг 4. Проверка прогресса по целям

Прочитай `## Текущие цели` из USER.md.

Для каждой цели:

- Посмотри, упоминается ли она в wins или tomorrow_intention за неделю
- Если ни разу не упоминается → статус `⚠️ нет активности`
- Если упоминается в wins → статус `✅ есть прогресс`
- Если упоминается только в blockers → статус `🔴 есть препятствие`

---

### Шаг 5. Формирование отчёта

Составь сообщение по шаблону ниже. Текст должен быть конкретным — только цифры и факты из данных, без воды.

**Формат сообщения (отправить в личку Маргулану):**

```
📊 Аналитик — итоги недели [ДД.ММ–ДД.ММ]

⚡ Энергия: X.X/10 [тренд]
😊 Настроение: X.X/10 [тренд]
🎯 Фокус: X.X/10 [тренд]

Лучший день: [день недели] ([дата])
[если есть выраженный худший день] Тяжёлый день: [день недели]

🏆 Паттерн побед:
• [повторяющийся win или топ-3 wins если уникальные]

🧱 Системный blocker:
• [повторяющийся blocker или "разные — паттерна нет"]

🎯 Прогресс по целям:
• [Цель 1]: [статус]
• [Цель 2]: [статус]

💡 Инсайт: [1 конкретный вывод из данных — что значит эта комбинация метрик]
```

**Правила форматирования:**

- Метрики округлять до одного знака после запятой
- Тренд показывать рядом с метрикой: `7.3/10 ↑`
- Инсайт — 1-2 предложения, конкретно (не "ты молодец")
- Если данных для цели недостаточно — написать "нет данных"
- Максимальная длина сообщения — 30 строк

---

### Шаг 6. Сохранение в файл

Добавить запись в `data/reflections-YYYY-MM.json`, поле `weekly_stats`:

```json
{
  "week": "YYYY-WNN",
  "period": { "from": "YYYY-MM-DD", "to": "YYYY-MM-DD" },
  "records_count": N,
  "avg_energy": X.X,
  "avg_mood": X.X,
  "avg_focus": X.X,
  "trend_energy": "растёт|падает|стабильно",
  "trend_mood": "растёт|падает|стабильно",
  "trend_focus": "растёт|падает|стабильно",
  "top_wins": ["..."],
  "top_blockers": ["..."],
  "goals_status": { "цель 1": "прогресс|нет активности|препятствие", ... },
  "insight": "...",
  "generated_at": "ISO8601"
}
```

После сохранения → `/git` для синхронизации.

---

## Формат ответа (при ручном вызове)

Отправить отчёт в личку по тому же шаблону, затем:

```
🛡️ Аудит [Аналитик]:
- Данные: data/reflections-YYYY-MM.json ✅
- Записей проанализировано: N
- Статус: отчёт сформирован и сохранён
```
