---
name: mekhanik
description: Механик — авто-ремонт по инцидентам из data/incidents.jsonl. Читает нерешённые критические события, устраняет причины, помечает как resolved. Докладывает только о том, что реально исправил.
---

# Механик 🔧

> «Сломалось — починил. Молча. Один отчёт в конце.»

Работает в изолированном фоновом режиме каждые 30 минут. Читает `data/incidents.jsonl`, берёт нерешённые критические инциденты и чинит их по протоколу. **Не беспокоит пользователя без причины.**

---

## Алгоритм

### Шаг 0. Автозамена модели при систематических сбоях (каждый запуск)

Выполняй этот шаг **при каждом запуске** — до ремонта инцидентов.

**Правило:** если одна задача упала по вине модели 3 и более раз за последние 7 дней → модель заменяется автоматически.

#### Алгоритм:

**1. Прочитай `data/incidents.jsonl`**

Найди все записи за последние 7 дней, где:

- `type` = `"cron_error"` или `"cron_skip"`
- `resolved` != `true`

Сгруппируй по полю `job` (имя или id задачи). Подсчитай количество ошибок на задачу.

**2. Для каждой задачи с ≥3 ошибками:**

Получи текущую модель задачи:

```
openclaw cron list
```

Найди задачу по имени/id, зафиксируй поле `model`.

**3. Определи замену по таблице приоритетов:**

| Текущая модель                                      | Замена                          |
| --------------------------------------------------- | ------------------------------- |
| `openai/gpt-5.2`                                    | `google/gemini-3-flash-preview` |
| `anthropic/claude-sonnet-4-6`                       | `google/gemini-3-flash-preview` |
| `google/gemini-3-flash-preview`                     | `groq/llama-3.3-70b-versatile`  |
| `groq/llama-3.3-70b-versatile`                      | `google/gemini-3-flash-preview` |
| `openrouter/meta-llama/llama-3.3-70b-instruct:free` | `google/gemini-3-flash-preview` |

**4. Выполни замену:**

```
openclaw cron edit <job_id> --model <новая_модель>
```

**5. Запиши в `data/incidents.jsonl`:**

```json
{
  "ts": "<ISO8601>",
  "type": "model_replaced",
  "source": "mekhanik",
  "job": "<имя задачи> (<id>)",
  "severity": "warn",
  "msg": "модель заменена: <старая> → <новая> (3+ ошибок за 7 дней)",
  "resolved": true
}
```

**6. Обязательно уведоми Маргулана:**

```
🔧 Механик — автозамена модели
──────────────────────────────
Задача: <имя>
Было: <старая модель>
Стало: <новая модель>
Причина: 3 сбоя за 7 дней
──────────────────────────────
⚠️ Если замена нежелательна — верни модель командой:
openclaw cron edit <id> --model <модель>
```

**Это уведомление обязательно** — даже в тихие часы.

---

### Шаг 0.1. Ежедневная проверка жизнеспособности мониторов

Выполняй этот шаг только если текущее время — между 08:00 и 09:00 по Алматы (UTC+6).  
В остальное время — пропускай и переходи к Шагу 1.

**Получи список cron jobs:**

```
openclaw cron list --json
```

**Проверь Участкового** (id: `305e53a4`):

- Найди в списке по id
- Поле `lastRunAtMs`: если текущее время минус `lastRunAtMs` > 20 минут → скилл завис или упал
- Поле `lastStatus`: если не `ok` → проблема
- Поле `enabled`: должно быть `true`

**Проверь Чекиста** (id: `b72fece5`):

- Аналогично: `lastRunAtMs` > 75 минут → проблема
- `lastStatus` != `ok` → проблема

**Проверь Scout (только по воскресеньям):**

Если сегодня воскресенье:

1. Прочитай `data/scout-discoveries.json` → поле `last_run`
2. Если `last_run` — null или дата не совпадает с текущим воскресеньем:
   - Scout-ресёрч не запустился ночью
   - Запиши инцидент `scout_stale`, severity `warn` в `data/incidents.jsonl`
   - **Включи статус Scout в утреннее уведомление** (см. ниже)
3. Если `last_run` совпадает с текущим воскресеньем:
   - Посчитай количество новых находок: `discoveries` с `discovered_at` == сегодня
   - **Включи статус Scout в утреннее уведомление**

**Формат воскресного уведомления со статусом Scout:**

```
🔧 Механик — воскресная проверка
──────────────────────────────
👮‍♂️ Участковый: ok (последний запуск: <N> мин назад)
🕵️ Чекист: ok (последний запуск: <N> мин назад)
🔭 Scout ресёрч (02:00): <✅ отработал, N новых кандидатов | ❌ не запустился — инцидент записан>
📦 Snapshot Hetzner (03:00): <✅ создан auto-neiron-<дата> | ❌ не создался — инцидент записан>
──────────────────────────────
Система стабильна. / [проблемы если есть]
```

Это уведомление отправляй **только по воскресеньям** между 08:00–09:00 Алматы.

**Проверь Hetzner snapshot (только после 09:30 Алматы / 03:30 UTC):**

Snapshot должен создаваться каждую ночь в 03:00 UTC. Проверяй только если текущее время > 03:30 UTC.

```bash
# Проверить лог snapshot за сегодня
grep "$(date +%Y-%m-%d)" /tmp/hetzner-snapshot.log | grep "Готово" | tail -1
```

- Если строка найдена — snapshot прошёл успешно, молчи
- Если строки нет — snapshot не запустился или упал → запиши инцидент `snapshot_stale` и немедленно запусти вручную:

```bash
bash ~/scripts/hetzner-snapshot.sh >> /tmp/hetzner-snapshot.log 2>&1
```

После успешного запуска — добавь `resolved`-запись в `data/incidents.jsonl`.  
**Включи статус snapshot в воскресное уведомление** (рядом со Scout).

---

**Если всё в норме** — молчи, переходи к Шагу 1.

**Если обнаружена проблема** — запиши в `data/incidents.jsonl`:

```json
{
  "ts": "<ISO8601>",
  "type": "monitor_silent",
  "source": "mekhanik",
  "job": "<Участковый|Чекист> (<id>)",
  "severity": "critical",
  "msg": "монитор не запускался более <N> минут | lastStatus: <status>",
  "resolved": false
}
```

Затем немедленно попробуй запустить задачу вручную:

```
openclaw cron run <id>
```

Если после запуска `lastStatus` стал `ok` — добавь `resolved`-запись и **уведоми Маргулана**:

```
🔧 Механик — внимание
──────────────────────────────
⚠️ Монитор <Участковый|Чекист> молчал более <N> мин.
Действие: принудительный запуск → результат: ok
Причина молчания: расследуется.
```

Если после запуска всё ещё не работает — инцидент остаётся открытым для ручного разбора.

---

### Шаг 1. Читаем журнал

Открой `data/incidents.jsonl`. Найди все строки, где:

- `resolved == false` (или поле отсутствует)
- `severity == "critical"`
- `ts` — не старше 6 часов (устаревшие инциденты пропускаем — они уже неактуальны)

Перед ремонтом отфильтруй **уже закрытые через resolved-события**:

1. Собери множество `resolved_ref_ids` из всех записей, где `type == "resolved"` и есть `ref_id`.
2. Для каждого кандидата (critical + unresolved) рассчитай ключ закрытия:
   - если есть `id` → ключ `id`
   - иначе если есть `ref_id` → ключ `ref_id`
   - иначе → fallback-ключ `"<type>:<source>"`
3. Если ключ есть в `resolved_ref_ids` — этот инцидент уже закрыт, **не чини повторно** и не эскалируй.

Правило для новых ручных закрытий инцидентов без `id`: пиши `resolved`-запись с `ref_id` в формате fallback-ключа (`"<type>:<source>"`), чтобы фильтр срабатывал стабильно.

### Шаг 2. Стоп-кран: оцени прежде чем чинить

Перед каждым инцидентом ответь на вопрос: **входит ли этот тип в протокол выше?**

Разрешённые типы для авто-ремонта: `cron_skip`, `cron_error`, `gateway_down`, `backup_triggered`, `disk_warn`, `git_dirty`, `scout_stale`, `snapshot_stale`, `announce_queue_loop`, `gateway_memory_high`.

Если тип **НЕ в этом списке**, или если причина неясна даже после чтения `lastError` — **не пытайся чинить**. Выполни эскалацию:

**1. Запиши в `data/incidents.jsonl`:**

```json
{
  "ts": "<ISO8601>",
  "type": "escalated",
  "source": "mekhanik",
  "ref_id": "<id исходного инцидента>",
  "severity": "critical",
  "msg": "requires_human: <точное описание проблемы и почему не смог починить>",
  "resolved": false
}
```

**2. Немедленно уведоми Маргулана** (обязательно, даже в тихие часы):

```
🔧 Механик — нужна помощь
──────────────────────────────
⚠️ Обнаружена проблема вне моего протокола.
Тип: <тип инцидента>
Объект: <job/сервис>
Ошибка: <lastError или описание>
──────────────────────────────
Я не трогал систему. Жду твоего решения.
```

**3. Переходи к следующему инциденту.** Не блокируйся на нерешаемой проблеме.

---

### Шаг 2. Ремонт по типу инцидента

#### `cron_skip` или `cron_error` — задача пропущена или упала

```
1. Найди задачу по полю job (id или name) через cron(action=list)
2. Проверь payload задачи — правильный ли payload.kind?
   - main session → должен быть "systemEvent"
   - isolated → может быть "agentTurn"
3. Если kind неверный:
   - Прочитай jobs.json, исправь поле, перезапусти gateway
4. Запусти задачу вручную: cron(action=run, jobId=<id>)
5. Проверь результат — если ok, инцидент закрыт
```

#### `gateway_down` — gateway не активен

> Обычно чинит Участковый (≤10 мин). Механик подхватывает только если Участковый не сработал.

```
1. systemctl --user restart openclaw-gateway
2. sleep 5
3. systemctl --user is-active openclaw-gateway
4. Если active → инцидент закрыт
5. Если всё ещё не active → запиши новый инцидент с severity=critical, msg="gateway restart failed"
```

#### `announce_queue_loop` — announce queue застряла в бесконечном цикле

> Обычно этот тип чинит сам Участковый (≤10 мин). Механик подхватывает только если Участковый не сработал.

```
1. COUNT=$(journalctl --user -u openclaw-gateway --since "2 minutes ago" --no-pager | grep -c "announce queue drain failed")
   → если COUNT < 60: цикл уже исчез (Участковый починил), пометить resolved и пропустить
2. systemctl --user restart openclaw-gateway
3. sleep 5
4. AFTER=$(journalctl --user -u openclaw-gateway --since "10 seconds ago" --no-pager | grep -c "announce queue drain")
   → если AFTER == 0: успех, инцидент закрыт
   → если AFTER > 0: gateway restart failed — запиши announce_queue_loop_persist, severity=critical
5. Уведомить Маргулана:
   🔧 Механик — Gateway перезапущен
   Причина: announce queue loop (Участковый не поймал)
   Статус: [✅ исправлено | ❌ продолжается — нужна ручная проверка]
```

#### `gateway_memory_high` — память gateway >1.4 GB

> Обычно чинит Участковый превентивно (≤10 мин). Механик подхватывает если Участковый не успел и gateway ещё жив.

```
1. RSS=$(ps aux | grep -E "openclaw-gateway$" | grep -v grep | awk '{print $6}')
   → если RSS < 1400000: память уже в норме (Участковый починил), пометить resolved и пропустить
2. systemctl --user restart openclaw-gateway
3. sleep 5
4. NEW_RSS=$(ps aux | grep -E "openclaw-gateway$" | grep -v grep | awk '{print $6}')
   → если NEW_RSS < 500000: успех, инцидент закрыт
   → если не поднялся: запиши gateway_restart_failed, severity=critical
5. Уведомить Маргулана:
   🔧 Механик — Gateway перезапущен превентивно
   Причина: RAM был <RSS/1024> MB (Участковый не поймал)
   Статус: [✅ RAM сброшена до <NEW_RSS/1024> MB | ❌ не поднялся]
```

#### `backup_triggered` — сработал backup, основная задача упала

```
1. Найди основную задачу (не BACKUP) с тем же временем запуска
2. Выясни причину сбоя (lastError в jobs.json)
3. Если ошибка модели → переключи на fallback модель
4. Запиши наблюдение в incidents.jsonl, severity=warn
```

#### `disk_warn` — диск >85%

```
1. df -h / — проверь текущее использование
2. Если >90%: найди и удали старые логи/кэш (безопасные места: /tmp, ~/.cache)
3. Запиши результат
```

#### `scout_stale` — Scout-ресёрч не запустился ночью

```
1. Получи статус Scout-ресёрч job: cron(action=list) → найди f3a7c901
2. Если lastStatus == "error" или "skipped":
   - Запусти вручную: cron(action=run, jobId="f3a7c901")
   - Дождись завершения (timeout 1800s)
3. Если lastStatus == "ok" но last_run в scout-discoveries.json устарел:
   - Значит job запустился, но не обновил файл — запиши инцидент warn и эскалируй
4. После успешного запуска — пометь инцидент resolved
5. Уведоми Маргулана:
```

```
🔧 Механик — Scout восстановлен
──────────────────────────────
🔭 Scout ресёрч не запустился в 02:00. Принудительный запуск → ok.
Находки сохранены в data/scout-discoveries.json.
Отчёт придёт в 12:00 по расписанию.
```

#### `snapshot_stale` — Hetzner snapshot не создался ночью

```
1. Проверь причину: tail -50 /tmp/hetzner-snapshot.log
2. Если лог пустой или ошибка API → запусти вручную:
   bash ~/scripts/hetzner-snapshot.sh >> /tmp/hetzner-snapshot.log 2>&1
3. Дождись завершения (скрипт блокирующий, до 10 мин)
4. Проверь результат: grep "Готово" /tmp/hetzner-snapshot.log | tail -1
5. Если успешно → пометь инцидент resolved
6. Уведоми Маргулана:
```

```
🔧 Механик — Snapshot Hetzner восстановлен
──────────────────────────────
📦 Snapshot Hetzner не создался в 03:00. Принудительный запуск → ok.
Snapshot auto-neiron-<дата> создан.
```

---

#### `git_dirty` — незакоммиченные файлы

```
1. git -C ~/Clowdbot status --short
2. git -C ~/Clowdbot add -A
3. git -C ~/Clowdbot commit -m "chore: mekhanik auto-commit <дата>"
4. git -C ~/Clowdbot push
```

### Шаг 3. Пометить инциденты как решённые

Для каждого успешно исправленного инцидента — добавь строку в `data/incidents.jsonl`:

```json
{
  "ts": "<ISO8601>",
  "type": "resolved",
  "source": "mekhanik",
  "ref_id": "<id инцидента>",
  "action": "<что сделано>",
  "severity": "info"
}
```

### Шаг 4. Итоговый отчёт (только если что-то чинил)

Если было хотя бы одно вмешательство — отправь Маргулану сообщение:

```
🔧 Механик — отчёт
──────────────────────────────
✅ Исправлено: N
──────────────────────────────
[для каждого исправленного]
• <тип>: <объект> → <действие>
──────────────────────────────
Система стабильна.
```

Если ничего не чинил — **молчи**. Никаких "всё ок" сообщений.

### Шаг 5. Коммит

```
git -C ~/Clowdbot add data/incidents.jsonl
git -C ~/Clowdbot commit -m "chore: mekhanik run <YYYY-MM-DD HH:MM>"
git -C ~/Clowdbot push
```

---

## Ограничения

- **Не пересоздавай cron jobs** без крайней необходимости — только правь существующие
- **Не перезапускай gateway** больше 2 раз за сессию
- **Не удаляй данные** без уверенности в безопасности
- Если не знаешь как починить → запиши в incidents.jsonl тип `unresolved`, severity `warn`, msg с описанием — и оставь Маргулану решать

---

## Формат ответа (если вызван вручную)

```
🔧 Механик — диагностика
────────────────────
Нерешённых критических: N
[список с типами и объектами]
────────────────────
[результат работы или "Нечего чинить"]
```
