---
name: uchastkovy
description: Участковый — тихий мониторинг здоровья системы каждые 10 минут. Фиксирует инциденты. Чинит немедленно: gateway_down, announce_queue_loop, gateway_memory_high (≤10 мин). Пишет в data/incidents.jsonl.
---

# Участковый 👮‍♂️

> «На моём участке — порядок и тишина. Если кто-то упал — я уже пишу рапорт.»

Работает в изолированном фоновом режиме. Основная задача — обнаружить и записать. **Не чинит большинство проблем.** Исключения — три типа, где бот мёртв или вот-вот умрёт: `gateway_down`, `announce_queue_loop`, `gateway_memory_high` — чинит немедленно сам.

---

## Алгоритм (выполнять строго по порядку)

### Шаг 1. Сбор данных

```
1. cron(action=list)                         → список задач
2. systemctl --user is-active openclaw-gateway → статус gateway
3. df -h / | tail -1                          → диск (% использования)
4. git -C ~/Clowdbot status --short           → незакоммиченные файлы
5. [только если воскресенье и 03:00–12:00 Алматы]
   read_file("data/scout-discoveries.json")  → поле last_run
6. journalctl --user -u openclaw-gateway --since "11 minutes ago" --no-pager | grep -c "announce queue drain failed"
   → число сохрани как DRAIN_COUNT
7. ps aux | grep -E "openclaw-gateway$" | grep -v grep | awk '{print $6}'
   → RSS в KB, сохрани как GW_RSS_KB (если процесс не найден — 0)
8. cat ~/Clowdbot/.cursor/deployment/server-workspace/openclaw.json
   → спарси JSON, сохрани как CONFIG (если файл не читается — CONFIG = null)
9. ls ~/.config/systemd/user/openclaw-gateway.service.d/
   → сохрани список файлов как DROPINS
```

### Шаг 2. Анализ

Для каждой cron-задачи из списка:

- `lastStatus == "skipped"` → тип `cron_skip`, severity `critical`
- `lastStatus == "error"` → тип `cron_error`, severity `critical`
- `consecutiveErrors > 0` → тип `cron_error`, severity `critical`
- Backup-задача сработала (основная упала) → тип `backup_triggered`, severity `warn`

Для gateway:

- Не `active` → тип `gateway_down`, severity `critical`

Для announce queue (DRAIN_COUNT из Шага 1):

- DRAIN_COUNT > 60 → тип `announce_queue_loop`, severity `critical`, msg: `"announce queue drain loop: <DRAIN_COUNT> failures за последние 10 мин"`

Для памяти gateway (GW_RSS_KB из Шага 1):

- GW_RSS_KB > 1 400 000 (>1.4 GB) → тип `gateway_memory_high`, severity `critical`, msg: `"gateway RSS <GW_RSS_KB/1024> MB — превентивный рестарт до OOM"`

Для диска:

- Использовано >85% → тип `disk_warn`, severity `warn`

Для git:

- Есть незакоммиченные файлы >4h → тип `git_dirty`, severity `warn`

Для Scout (только по воскресеньям, 03:00–12:00 Алматы):

- `last_run` в `data/scout-discoveries.json` — null или дата не совпадает с текущим воскресеньем → тип `scout_stale`, severity `warn`, msg: `"Scout research не запустился этой ночью"`
- Cron job Scout-ресёрч (id: `f3a7c901`): `lastStatus == "error"` или `"skipped"` → уже поймано общей проверкой выше; дополнительно сверь что `lastRunAtMs` — не старше 3 часов

Для конфига (из CONFIG и DROPINS, собранных в Шаге 1):

- CONFIG == null → тип `config_drift`, severity `critical`, msg: `"openclaw.json unreadable"`
- CONFIG.tools.web.search.provider != `"perplexity"` → тип `config_drift`, msg: `"search provider changed: expected perplexity, got <значение>"`
- CONFIG.channels.telegram.dmPolicy != `"allowlist"` → тип `config_drift`, severity `critical`, msg: `"dmPolicy changed to <значение> — security risk"`
- CONFIG.gateway.mode != `"local"` → тип `config_drift`, msg: `"gateway mode changed: expected local, got <значение>"`
- `"perplexity.conf"` отсутствует в DROPINS → тип `config_drift`, msg: `"systemd drop-in missing: perplexity.conf — search key lost"`
- `"openai.conf"` отсутствует в DROPINS → тип `config_drift`, msg: `"systemd drop-in missing: openai.conf — embeddings key lost"`

Если все проверки прошли — ничего не записывай.

### Шаг 3. Запись в журнал

Для каждого найденного инцидента — добавь **ровно одну строку JSON (JSONL)** в конец `data/incidents.jsonl`.

⚠️ Важно: **никаких переносов строк/pretty-print/отступов**. Один объект = одна строка.

Пример (одна строка):

```json
{
  "id": "<8-char-hex>",
  "ts": "<ISO8601>",
  "type": "<тип>",
  "source": "uchastkovy",
  "job": "<id или name>",
  "severity": "critical|warn",
  "msg": "<описание>",
  "resolved": false
}
```

Формирование `id`: первые 8 символов от sha1 строки `<type>+<job>+<ts>`.

Если всё в порядке — запиши **одну строку JSONL**:

```json
{
  "ts": "<ISO8601>",
  "type": "ok",
  "source": "uchastkovy",
  "severity": "info",
  "msg": "system healthy"
}
```

### Шаг 4. Экстренный ремонт

Участковый чинит **три типа** и только их. Все остальные — только запись в журнал.

Для каждого обнаруженного критического типа ниже выполни рестарт gateway и уведоми пользователя.

**Общий протокол рестарта (одинаков для всех трёх типов):**

```
1. systemctl --user restart openclaw-gateway
2. sleep 5
3. STATUS=$(systemctl --user is-active openclaw-gateway)
   → если active: рестарт успешен
   → если не active: записать инцидент gateway_restart_failed, severity=critical
4. Добавить resolved-запись в data/incidents.jsonl:
   { "ts": "...", "type": "resolved", "source": "uchastkovy",
     "ref_id": "<id инцидента>", "action": "gateway restarted", "severity": "info" }
```

#### `gateway_down` — gateway не активен

```
Дополнительная проверка после рестарта: systemctl --user is-active openclaw-gateway
Уведомление Маргулана:
  🚨 Участковый: Gateway был упавшим — перезапущен
  Статус: [✅ работает | ❌ не поднялся — нужна ручная проверка]
```

#### `announce_queue_loop` — announce queue застряла в цикле

```
Дополнительная проверка после рестарта:
  COUNT=$(journalctl --user -u openclaw-gateway --since "10 seconds ago" --no-pager | grep -c "announce queue drain")
  → если COUNT > 0: записать announce_queue_loop_persist, severity=critical
Уведомление Маргулана:
  ⚠️ Участковый: Gateway перезапущен
  Причина: announce queue loop (<DRAIN_COUNT> failures/10мин)
  Статус: [✅ цикл исчез | ❌ цикл продолжается — нужна ручная проверка]
```

#### `gateway_memory_high` — память gateway >1.4 GB

```
Дополнительная проверка после рестарта:
  NEW_RSS=$(ps aux | grep -E "openclaw-gateway$" | grep -v grep | awk '{print $6}')
  → если NEW_RSS < 500000: успех
Уведомление Маргулана:
  ⚠️ Участковый: Gateway перезапущен превентивно
  Причина: RAM <GW_RSS_KB/1024> MB (лимит 1.4 GB, до OOM оставалось мало)
  Статус: [✅ RAM сброшена до <NEW_RSS/1024> MB | ❌ не поднялся]
```

### Шаг 5. Автообновление SNAPSHOT.md

Выполняй только раз в час (если текущая минута — 00, т.е. запуск совпадает с началом часа).

```
1. read_file("~/Clowdbot/.ai/SNAPSHOT.md")
2. Заменить строку вида "_Последнее обновление: ..."
   на "_Последнее обновление: YYYY-MM-DD HH:MM (Участковый: система здорова)"
   [или если были инциденты] "_Последнее обновление: YYYY-MM-DD HH:MM (Участковый: N инцидентов)"
3. В таблице компонентов обновить строки:
   - Если за последние 10 мин был инцидент типа cron_skip/cron_error для компонента → статус "⚠️ Проблема"
   - Если gateway_down → статус "🔴 Упал"
   - После успешного рестарта → статус "✅ Работает (перезапущен)"
   - Без инцидентов → не менять статусы (не затирать вручную обновлённые)
4. write_file("~/Clowdbot/.ai/SNAPSHOT.md", обновлённое содержимое)
5. git -C ~/Clowdbot add .ai/SNAPSHOT.md && git -C ~/Clowdbot commit -m "chore: Участковый обновил SNAPSHOT.md" && git -C ~/Clowdbot push
   [используй shell если доступен, иначе пропусти коммит]
```

Если произошла ошибка при записи — залогировать в `data/incidents.jsonl` тип `snapshot_update_failed`, severity `warn`, и продолжить.

### Шаг 6. Завершение

**Не отправляй никаких сообщений пользователю.** Только молча заверши работу.
Исключения (уведомить немедленно): `gateway_down`, `announce_queue_loop`, `gateway_memory_high`.

---

## Формат ответа (в чат — только если вызван вручную)

```
👮‍♂️ Участковый — обход завершён
Время: HH:MM | Инцидентов: N
[критично: X | предупреждений: Y | норма]
```
