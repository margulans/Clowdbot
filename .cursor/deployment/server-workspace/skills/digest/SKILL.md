---
name: digest
description: Внеочередной дайджест новостей по требованию
command-description: Внеочередной дайджест новостей
user-invocable: yes
---

# digest

> **ОБНОВЛЕНИЕ:** Для глубокого анализа конкретных URL используй `/smart-digest` —
> он открывает страницу в браузере, делает скриншот, извлекает контент и анализирует
> позицию автора. Используй `/digest` для автоматического сбора новостей по ключевым словам,
> `/smart-digest` — когда есть конкретные URL для анализа.

Пользователь запрашивает **внеочередной дайджест** новостей прямо сейчас.

## Инструкции:

1. **Прочитай** `data/sent-digests.json` для дедупликации
2. Собери 10-15 **свежих** новостей (только за последние 48 часов!) по приоритетным темам
3. Используй Perplexity для поиска (см. раздел "Стратегия поиска" ниже)
4. Отправь дайджест СТРОГО по формату из `digest-format-final.md`
5. **НЕ добавляй inline кнопки** — оценка через нативные реакции Telegram
6. Минимум 30% новостей из новых источников
7. **После отправки** запиши отправленные новости в `data/sent-digests.json`
8. **После каждого sendMessage** сохрани message_id в `data/dual-rating-data.json` → `messageHistory`
9. **В конце дайджеста** отправь блок «🛡️ АУДИТ ДАЙДЖЕСТА» (см. `digest-format-final.md`)

## Если пользователь указал тему:

- Сфокусируй дайджест на указанной теме
- Например: `/digest ИИ` — только новости про ИИ

## Если пользователь указал конкретные URL:

- Перенаправь на `/smart-digest` — он сделает глубокий анализ через браузер

---

## Стратегия поиска (Perplexity как основной)

Perplexity возвращает **синтезированный ответ + источники** — не просто ссылки.
Используй это: задавай вопросы, а не ключевые слова.

### Запросы по темам (задавай именно так):

```
web_search("What are the most significant AI and LLM developments in the last 24 hours?")
web_search("Latest robotics and humanoid robot news today — breakthroughs, releases, funding")
web_search("eVTOL and urban air mobility news in the last 24 hours — new aircraft, certifications, investments")
web_search("Top technology and vibe coding news today — tools, frameworks, developer ecosystem")
web_search("Major business and investment news in tech sector today — deals, funding rounds, IPO")
```

### Дополнительные запросы при недостатке материала:

```
web_search("AI companies announcements and product launches last 24 hours")
web_search("OpenAI Anthropic Google DeepMind news today")
web_search("Robotics startup funding and product release news this week")
```

### Как использовать ответ Perplexity:

- Perplexity даёт сводку + список источников с URL — используй оба
- Из сводки — суть события (не пересказывай дословно, дай своё резюме)
- Из источников — конкретный URL для ссылки в дайджесте
- Если источник за пейволлом — используй `web_fetch` на URL для извлечения открытого контента

---

## Fallback при сбое Perplexity

### Уровень 1 — Brave Search API (через скрипт)

Если `web_search` (Perplexity) вернул ошибку/лимит/пусто → используй Brave Search API через локальный скрипт (он берёт ключ из `BRAVE_API_KEY` в env сервиса):

```bash
node /home/openclaw/.openclaw/workspace/scripts/brave-search.mjs "AI news today" 10
node /home/openclaw/.openclaw/workspace/scripts/brave-search.mjs "robotics news today" 10
```

Дальше: выбери 1–2 лучших URL из результатов и для каждого сделай `web_fetch(url)` чтобы подтвердить/прочитать контент перед публикацией.

### Уровень 2 — RSS

Если и Brave недоступен → `web_fetch` по лентам:

| Источник               | RSS URL                                                    | Тема          |
| ---------------------- | ---------------------------------------------------------- | ------------- |
| MIT Technology Review  | `https://www.technologyreview.com/feed/`                   | ИИ            |
| VentureBeat AI         | `https://venturebeat.com/category/ai/feed/`                | ИИ/Бизнес     |
| TechCrunch             | `https://techcrunch.com/feed/`                             | Технологии    |
| IEEE Spectrum Robotics | `https://spectrum.ieee.org/feeds/topic/robotics.rss`       | Робототехника |
| eVTOL.com              | `https://evtol.com/feed/`                                  | eVTOL         |
| Ars Technica           | `https://feeds.arstechnica.com/arstechnica/technology-lab` | Технологии    |

Извлеки `<title>`, `<link>`, `<pubDate>` из XML. Фильтруй за последние 48ч.
В конце дайджеста добавь пометку соответствующего уровня:

- `⚠️ Perplexity недоступен — использован Brave`
- `⚠️ Поиск недоступен — использованы RSS`

---

## Формат ответа:

Используй формат из `digest-format-final.md`. Не отправляй итоговое сообщение.
