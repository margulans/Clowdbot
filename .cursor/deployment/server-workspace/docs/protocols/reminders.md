# Reminder Protocol (Маргулан ↔ Нейрон)

## Цель
Любое напоминание, которое Маргулан просит «сделать/поставить», должно **гарантированно прийти в Telegram** как обычное сообщение, а не остаться внутренним systemEvent.

## Обязательное правило
Любой user‑requested reminder создаётся **только** как cron job:
- `sessionTarget = "isolated"`
- `payload.kind = "agentTurn"`
- внутри payload выполняется явная отправка:
  `message(action=send, channel=telegram, to=685668909, message="...")`

НЕЛЬЗЯ для user‑requested reminders:
- `payload.kind=systemEvent`
- `sessionTarget=main`

## Минимальный шаблон job
```json
{
  "name": "🔔 Reminder: <title>",
  "schedule": { "kind": "at", "at": "<ISO-UTC>" },
  "sessionTarget": "isolated",
  "payload": {
    "kind": "agentTurn",
    "model": "openai/gpt-4o-mini",
    "thinking": "low",
    "timeoutSeconds": 120,
    "message": "Это напоминание (запланировано заранее).\n\n<текст>\n\n[[DELIVERY_VIA_MESSAGE_TOOL]]\nmessage(action=send, channel=telegram, to=685668909, message=\"<текст>\")"
  },
  "delivery": { "mode": "none" },
  "enabled": true
}
```

## Тихие часы
Если напоминание не критичное и попадает в 23:00–06:00 Алматы — в тексте явно помечать «не срочно» и при необходимости переносить время (только с подтверждением).
