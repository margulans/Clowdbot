# Система исследования новых источников (Exploration vs Exploitation)

## Концепция
Баланс между проверенными источниками (exploitation) и исследованием новых (exploration) для постоянного улучшения качества новостных дайджестов.

## Правило 33% новых источников

### Структура дайджеста (10 новостей):
- **67% (7 новостей)** - проверенные источники с высокими рейтингами
- **33% (3 новости)** - новые/исследуемые источники

### Категории источников:

#### 🏆 Проверенные (Exploited Sources)
- **Рейтинг**: >1.5 балла средней оценки
- **Количество оценок**: >10 реакций
- **Статус**: Стабильно качественные
- **Примеры**: @serge_ai (после накопления оценок)

#### 🔍 Исследуемые (Exploration Sources)
- **Новички** (0-5 оценок) - совсем новые источники
- **Кандидаты** (5-10 оценок) - набирают статистику  
- **Ротируемые** - периодическая проверка старых источников

#### 🗑 Отклоненные (Rejected)
- **Рейтинг**: <0 баллов средней оценки
- **Статус**: Исключены из ротации
- **Возврат**: Через 3 месяца для повторной проверки

## Алгоритм отбора новостей

### Шаг 1: Сбор новостей
```python
def collect_news_with_exploration():
    all_news = []
    
    # Собираем новости из всех источников
    for source in all_sources:
        news = get_news_from_source(source)
        for article in news:
            article['source_category'] = get_source_category(source)
            article['source_rating'] = get_source_rating(source)
            all_news.append(article)
    
    return all_news
```

### Шаг 2: Категоризация по источникам
```python
def categorize_by_source_type(news_list):
    proven_sources = []      # Рейтинг >1.5, >10 оценок
    exploration_sources = [] # Новые или <10 оценок
    
    for article in news_list:
        source_stats = get_source_statistics(article['source'])
        
        if (source_stats['avg_rating'] > 1.5 and 
            source_stats['total_reactions'] > 10):
            proven_sources.append(article)
        else:
            exploration_sources.append(article)
    
    return proven_sources, exploration_sources
```

### Шаг 3: Применение правила 33%
```python
def select_news_with_33_rule(proven, exploration, total_limit=10):
    exploration_count = max(1, int(total_limit * 0.33))  # Минимум 33%
    proven_count = total_limit - exploration_count
    
    # Отбираем лучшие из проверенных
    selected_proven = select_top_news(proven, proven_count)
    
    # Отбираем исследуемые с приоритетом новизны
    selected_exploration = select_exploration_news(exploration, exploration_count)
    
    return selected_proven + selected_exploration
```

## Стратегии исследования новых источников

### 1. Автоматическое обнаружение
```python
discovery_methods = {
    "trending_hashtags": "Поиск по популярным хештегам",
    "related_channels": "Каналы, упоминаемые в качественных источниках", 
    "user_recommendations": "Каналы, которые добавляет Маргулан",
    "citation_analysis": "Источники, которые цитируют качественные каналы",
    "topic_clustering": "Поиск по ключевым словам в новых каналах"
}
```

### 2. Градация новых источников
```python
exploration_priority = {
    "user_added": 10,        # Каналы от Маргулана (высший приоритет)
    "highly_cited": 8,       # Часто цитируемые другими
    "trending": 6,           # Популярные по хештегам
    "topic_relevant": 4,     # Соответствуют темам интересов
    "random_discovery": 2    # Случайные находки
}
```

### 3. Ротационная система
- **Еженедельно**: 2-3 совершенно новых источника
- **Ежемесячно**: Возврат исключенных источников для переоценки
- **Сезонно**: Полный аудит всех источников

## Метрики системы exploration

### KPI успешности:
- **Discovery Rate**: >2 новых качественных источника в месяц
- **Retention Rate**: >50% новых источников получают положительные оценки
- **Diversity Index**: Разнообразие тематик и географии источников
- **Quality Growth**: Постоянный рост средней оценки дайджестов

### Отчетность для Маргулана:
```
📊 **Месячный отчет исследования источников**

🔍 **Исследовано**: 12 новых источников
✅ **Принято в ротацию**: 4 источника (33%)
🏆 **Повышены до проверенных**: 2 источника  
🗑 **Исключено**: 3 источника

**Лучшие находки месяца:**
1. @new_ai_channel - рейтинг 2.3 (ИИ)
2. evtol_daily - рейтинг 1.9 (eVTOL)

**Тренды качества:**
- Средний рейтинг: 1.2 → 1.6 (+33%)
- Разнообразие источников: +15%
```

## Защита от информационного пузыря

### Механизмы разнообразия:
1. **Географическое разнообразие**: источники из разных стран
2. **Языковое разнообразие**: русский + английский контент
3. **Форматное разнообразие**: новости, аналитика, интервью
4. **Временное разнообразие**: свежие новости + глубокая аналитика

### Алгоритм анти-пузыря:
```python
def ensure_diversity(selected_news):
    # Проверяем разнообразие по параметрам
    diversity_check = {
        'sources': len(set([n['source'] for n in selected_news])),
        'categories': len(set([n['category'] for n in selected_news])),
        'languages': len(set([n['language'] for n in selected_news])),
        'formats': len(set([n['format'] for n in selected_news]))
    }
    
    # Если разнообразие низкое - форсируем добавление разных источников
    if diversity_check['sources'] < 6:  # Из 10 новостей минимум 6 разных источников
        return force_source_diversity(selected_news)
    
    return selected_news
```

## Внедрение в систему

### Обновление приоритизации:
- **Проверенные источники**: базовый приоритет × рейтинг
- **Новые источники**: базовый приоритет × 1.2 (бонус за новизну)
- **Пользовательские**: базовый приоритет × 2.5 (как сейчас)

### Обновление лимитов:
```
Структура дайджеста (10 новостей):
├── 7 новостей из проверенных источников
│   ├── 3-4 ИИ/Робототехника (топ категории)  
│   ├── 2-3 eVTOL/Бизнес (средние категории)
│   └── 1-2 Инструменты (низший приоритет)
└── 3 новости из исследуемых источников
    ├── 1 новый источник (0-5 оценок)
    ├── 1 кандидат (5-10 оценок)  
    └── 1 ротационный/случайный
```

## Ожидаемый результат
- **Постоянный рост** качества источников
- **Предотвращение застоя** в информационном пузыре
- **Обнаружение** нишевых высококачественных источников
- **Адаптация** к изменяющемуся медиа-ландшафту
- **Сохранение разнообразия** при улучшении качества