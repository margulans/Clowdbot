# Формат дайджеста — Финальный формат для Telegram

## Заголовок дайджеста:

Первое сообщение дайджеста (ОБЯЗАТЕЛЬНО указывай время!):
```
🌅 **УТРЕННИЙ ДАЙДЖЕСТ** | 08.02.2026, 08:30
📊 12 новостей | 🎯 Только самые важные
```

Формат: `ДД.ММ.ГГГГ, ЧЧ:ММ` — время местное (по текущему timezone пользователя из MEMORY.md).

## Отправка каждой новости:

Отправляй ТОЛЬКО текст, БЕЗ inline кнопок:
```json
{
  "channel": "telegram",
  "action": "sendMessage",
  "to": "tg:685668909",
  "content": "🤖 **OpenAI анонсировала GPT-5**\n\nНовая модель получила возможности обработки видео, аудио, изображений и кода. GPT-5 показывает на 40% лучшие результаты в бенчмарках. Цена API снижена на 60%.\n\n📰 OpenAI Blog — openai.com/gpt5"
}
```

**НЕ добавляй `buttons`!** Оценка идёт через нативные реакции Telegram (emoji под сообщением).

## Оценка через нативные реакции Telegram:

Пользователь ставит реакцию (emoji) прямо на сообщение — зажатием или двойным тапом.
Бот автоматически получает `message_reaction` событие.

### Маппинг emoji → рейтинг:

| Реакция | Значение | Баллы |
|---------|----------|-------|
| 🔥 | Отлично | +10 |
| 👍 | Нравится | +5 |
| 👎 | Не нравится | -3 |
| 💩 | Мусор | -5 |

### Обработка реакции:

Ты получишь системное событие вида:
```
Telegram reaction added: 👍 by Margulan on msg 12345
```

Что делать:
1. Определи emoji и баллы по таблице выше
2. Обнови рейтинг источника новости
3. Ответь `[SILENT]` (не отправляй текстовый ответ)

Если emoji не входит в таблицу (❤️, 😂 и т.д.) — просто игнорируй, не обновляй рейтинг.

## Правила формата:

1. **Эмодзи + заголовок** — полная развёрнутая формулировка
2. **Описание** — 3-5 полных предложений, развёрнутое с деталями и цифрами
3. **Источник + ссылка** — `📰 Название — url`
4. **Без inline кнопок** — оценка через нативные реакции

## ⏰ Валидация свежести (ОБЯЗАТЕЛЬНО!)

Каждая новость **ДОЛЖНА** пройти проверку свежести перед включением в дайджест:

### Правила свежести:

1. **Проверь дату публикации** — новость должна быть опубликована **не ранее 48 часов назад**
2. **При веб-поиске** используй параметр `freshness: "pd"` (past day) или `"pw"` (past week)
3. **Если дата неизвестна** — проверь содержание: упоминаются ли конкретные даты, "сегодня", "вчера"?
4. **Старые новости** (>48ч) → **ПРОПУСТИТЬ**, даже если они интересные

### Признаки устаревшей новости:
- Дата публикации >2 дней назад
- Событие уже широко обсуждается давно
- Пресс-релиз или анонс без свежей даты
- "Переписанная" версия старой новости с другого сайта

### Исключения (можно включить старше 48ч):
- ✅ **Развитие событий** — новый поворот в истории (пометь "Обновление:")
- ✅ **Эксклюзив** — ранее недоступная информация
- ✅ **Аналитика** — глубокий анализ свежего тренда

### Проверка при поиске:
```
web_search({ query: "...", freshness: "pd" })  // предпочтительно
web_search({ query: "...", freshness: "pw" })  // запасной вариант
```

## 🔁 Дедупликация (ОБЯЗАТЕЛЬНО!)

Перед каждым дайджестом:

1. **Прочитай** файл `data/sent-digests.json`
2. **Сравни** каждую найденную новость с записями за последние 7 дней:
   - Совпадение URL → **ПРОПУСТИТЬ** (точный дупликат)
   - Совпадение ключевых слов заголовка (≥3 слова совпадают) → **ПРОПУСТИТЬ** (вероятный дупликат)
   - Та же тема/событие от другого источника → **ПРОПУСТИТЬ** (семантический дупликат)
3. **После отправки** дайджеста — **ЗАПИШИ** все отправленные новости в `data/sent-digests.json`

### Формат записи в sent-digests.json:

```json
{
  "entries": [
    {
      "date": "2026-02-07",
      "digest": "morning",
      "title": "OpenAI анонсировала GPT-5",
      "url": "https://openai.com/gpt5",
      "source": "OpenAI Blog",
      "category": "AI",
      "keywords": ["openai", "gpt-5", "анонс", "мультимодальность"]
    }
  ]
}
```

### Правила дедупликации:

- **URL-матч** → 100% дупликат, ВСЕГДА пропускать
- **Заголовок-матч** (≥3 значимых слова) → пропускать
- **Семантический матч** (та же новость, другой источник) → пропускать
- **Развитие событий** → МОЖНО отправить, если есть НОВЫЕ факты (укажи "Обновление:")
- **Хранить записи** за последние 7 дней, старые удалять при записи

### Пример проверки:

Вчера отправлено: "OpenAI выпустила GPT-5"
Сегодня найдено:
- ❌ "OpenAI представила GPT-5" → дупликат (те же ключевые слова)
- ❌ "GPT-5 от OpenAI меняет рынок" → дупликат (та же тема)
- ✅ "GPT-5 получил сертификацию в ЕС" → новый факт, можно отправить

## ЗАПРЕЩЕНО:
- `<tg-spoiler>` или `||spoiler||`
- Короткие однострочные описания
- Итоговое сообщение после дайджеста
- Отправлять текстовый ответ при получении реакции (только [SILENT])
- Добавлять `buttons` / inline keyboard к новостям
- **Отправлять новости, которые уже были в предыдущих дайджестах (за 7 дней)**
- **Отправлять новости старше 48 часов** (кроме исключений выше)