# Формат дайджеста — Финальный формат для Telegram

## Заголовок дайджеста:

Первое сообщение дайджеста:
```
🌅 **УТРЕННИЙ ДАЙДЖЕСТ** | 06.02.2026
📊 12 новостей | 🎯 Только самые важные
```

## Отправка каждой новости — ДВУХШАГОВЫЙ ПРОЦЕСС:

### Шаг 1: Отправь текст новости БЕЗ кнопок

Используй message tool:
```json
{
  "channel": "telegram",
  "action": "sendMessage",
  "to": "tg:685668909",
  "content": "🤖 **OpenAI анонсировала GPT-5 с полной мультимодальностью**\n\nНовая модель получила возможности обработки видео, аудио, изображений и кода в едином интерфейсе. GPT-5 показывает на 40% лучшие результаты в бенчмарках. Цена API снижена на 60%, что делает технологию доступнее для разработчиков.\n\n📰 OpenAI Blog — openai.com/gpt5"
}
```

Результат вернёт `messageId` и `chatId`. ЗАПОМНИ ИХ.

### Шаг 2: Добавь кнопки через editMessage

Используй message tool с тем же текстом + кнопки, где callback_data содержит chatId и messageId:
```json
{
  "channel": "telegram",
  "action": "editMessage",
  "chatId": "<chatId из шага 1>",
  "messageId": <messageId из шага 1>,
  "content": "<тот же текст что в шаге 1>",
  "buttons": [[
    {"text": "⭐ Отлично", "callback_data": "r:e:<chatId>:<messageId>"},
    {"text": "👍", "callback_data": "r:l:<chatId>:<messageId>"},
    {"text": "👎", "callback_data": "r:d:<chatId>:<messageId>"},
    {"text": "🗑️", "callback_data": "r:t:<chatId>:<messageId>"}
  ]]
}
```

## Обработка реакции (callback):

Когда получишь сообщение вида `r:e:<chatId>:<messageId>` или `r:l:...` и т.д.:

1. Определи реакцию:
   - `r:e` = Отлично (+10)
   - `r:l` = Нравится (+5)
   - `r:d` = Не нравится (-3)
   - `r:t` = Мусор (-5)

2. Запиши реакцию в систему рейтинга

3. Обнови кнопки — ПОДСВЕТИ выбранную через editMessage:
```json
{
  "channel": "telegram",
  "action": "editMessage",
  "chatId": "<chatId>",
  "messageId": <messageId>,
  "content": "<тот же текст новости>",
  "buttons": [[
    {"text": "✅ Отлично", "callback_data": "r:e:<chatId>:<messageId>"},
    {"text": "👍", "callback_data": "r:l:<chatId>:<messageId>"},
    {"text": "👎", "callback_data": "r:d:<chatId>:<messageId>"},
    {"text": "🗑️", "callback_data": "r:t:<chatId>:<messageId>"}
  ]]
}
```
   Добавь ✅ ТОЛЬКО к выбранной кнопке. Остальные оставь без изменений.

4. Ответь ТОЛЬКО: [SILENT]

## Правила формата:

1. **Эмодзи + заголовок** — полная развёрнутая формулировка
2. **Описание** — 3-5 полных предложений, развёрнутое с деталями и цифрами
3. **Источник + ссылка** — `📰 Название — url`
4. **Кнопки** — inline keyboard (добавляются через editMessage)

## ЗАПРЕЩЕНО:
- `<tg-spoiler>` или `||spoiler||`
- Короткие однострочные описания
- Итоговое сообщение после дайджеста
- Отправлять текстовый ответ при получении реакции (только [SILENT])
