# MEMORY.md - Долгосрочная память

## 👤 Пользователь

- **Имя:** Маргулан Сейсембай
- **Текущий timezone:** Asia/Almaty (UTC+6)

## 🏠 Инфраструктура

Технические детали сервера и ноды вынесены в `docs/infra/server-details.md`.

## 🔍 Мониторинг системы (активен с 2026-02-22)

Три фоновых скилла работают в изолированных cron-сессиях автономно:

| Скилл           | Cron          | Что делает                                                      |
| --------------- | ------------- | --------------------------------------------------------------- |
| 👮‍♂️ `uchastkovy` | каждые 10 мин | проверяет cron/gateway/диск/git, пишет в `data/incidents.jsonl` |
| 🕵️ `chekist`    | каждый час    | аудит `incidents.jsonl`, алерт при critical вне тихих часов     |
| 🔧 `mekhanik`   | каждые 30 мин | авто-ремонт нерешённых critical инцидентов                      |

- Журнал инцидентов: `data/incidents.jsonl` (append-only)
- Формат записи: `{"id":"...","ts":"...","type":"...","severity":"critical|warn|info","resolved":false}`
- Скилл `doctor` — удалён (2026-02-22), заменён тройкой выше

## 💰 Экономист (активен с 2026-02-22)

Фоновый аудитор эффективности и затрат. Работает в изолированных cron-сессиях, не мешает основной работе.

| Скилл          | Cron                  | Что делает                                                        |
| -------------- | --------------------- | ----------------------------------------------------------------- |
| 💰 `economist` | 23:30 Алматы (ежедн.) | собирает токены всех сессий, обновляет `data/economist-log.jsonl` |
| 📊 `economist` | вс 09:00 Алматы       | присылает недельный отчёт с рекомендациями по моделям и затратам  |

- Журнал затрат: `data/economist-log.jsonl` (append-only, по сессиям)
- Сводка: `data/cost-summary.json` (день/неделя/месяц по моделям — для мгновенных ответов)
- Ценник моделей: `data/model-pricing.json` (уточнять при изменении тарифов)
- Приоритеты: качество → надёжность → скорость → минимум затрат
- Модели не переключает сам — только рекомендует
- Алерт немедленно (даже ночью): если суточные расходы >$5 или одна сессия >300k токенов
- Ответ на вопросы о затратах: «сколько тратим», «статус затрат», «экономист»

## 🔒 Безопасность gateway (обновлено 2026-02-22)

- `gateway.bind` = `loopback` (127.0.0.1) — не торчит наружу
- `gateway.tools.allow` = `[]` — `sessions_send` убран
- Security audit: `0 critical · 1 warn` (warn — про trusted proxies, не критично)

## 📰 Новостной агрегатор

- **Новостной:** 08:00, 13:00, 18:00 (Алматы).
- **Мнения:** 08:30, 13:30, 18:30 (Алматы).
- **Обязательные авторы мнений:** Нассим Талеб, Рей Далио, Дарио Амадеи, Сэм Альтман, Майкл Левин, Питер Штайнбергер.

Подробные правила формата и дедупликации — в `digest-format-final.md`.

## 🛡️ Обязательный аудит

Блок технической прозрачности (🛡️) обязателен для всех сложных скиллов и дайджестов в каналах. Правила в `AGENTS.md`.

## 🔐 Безопасность и дисциплина

- Любое изменение через API/Cron — немедленный коммит в Git.
- Деструктивные команды только через подтверждение.
- Журнал важных решений перенесен в `memory/archive/decisions-2026-02.md`.

## 🎤 Голосовые сообщения

- Транскрипция: Groq Whisper Large V3.
- Статус: Работает.
