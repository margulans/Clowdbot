## Chat Cleanup — 16:01 UTC

### ✅ Сделано
- Проведён интенсивный heartbeat-аудит cron и git в течение дня.
- Выявлены сбои публикации:
  - **Дневной дайджест @newsneiron**: `lastStatus=error`, `consecutiveErrors=2` (часто с `An unknown error occurred`, периодически появлялся `runningAtMs`).
  - **Дневные мнения**: `lastStatus=error`, `consecutiveErrors=2` (таймаут `cron: job execution timed out`).
  - **Вечерний дайджест @newsneiron**: `lastStatus=error`, `consecutiveErrors=1` (unknown error), при этом **🚨 BACKUP: Вечерний дайджест** отработал `ok` (выпуск, вероятно, спасён).
- Зафиксировано, что **мониторинги** (Участковый/Чекист/Механик) в целом возвращались в `ok`, но были эпизоды «unknown error» у Участкового/Механика.
- Git-состояние в течение дня «плавало» (в т.ч. появлялись `memory.db`, изменения `incidents.jsonl`, экономист-логи), к моменту финальных проверок — **git status становился чистым**.

### 💡 Решения
- Для падающих/таймаутящихся дайджест-джоб:
  - рассмотреть **увеличение `timeoutSeconds`** (особенно для мнений/публикаций),
  - проверить, нет ли зависаний на шаге отправки сообщений/поиска,
  - держать бэкапы включенными (они реально спасают выпуски).
- По доставке: продолжать линию `delivery.mode=none` + явный `message()` (вместо `announce`) как более надёжный путь.

### 🐛 Проблемы
- Повторяющиеся ошибки cron вида **`An unknown error occurred`** без детализации — требуется диагностика (лог/пределы времени/ошибки отправки сообщений).
- У **Дневных мнений** регулярный таймаут (300s) выглядит недостаточным для полного цикла публикаций.

### ⏳ На завтра
- Прицельно разобрать причины ошибок у:
  - `Дневной дайджест @newsneiron (a0ed4696)`
  - `Дневные мнения (ba31c42d)`
  - (при повторении) `Вечерний дайджест (582cc3f0)`
- Поднять `timeoutSeconds` у «Дневных мнений» (и, при необходимости, у дайджестов новостей) до 600–900s.
- Проверить, почему некоторые рабочие джобы (например, **Участковый**) всё ещё настроены на `openai/gpt-4o-mini` (если модель запрещена/нестабильна в этой среде) и привести к единому стабильному набору моделей.
