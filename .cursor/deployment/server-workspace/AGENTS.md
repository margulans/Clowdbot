# AGENTS.md - Your Workspace

This is your core operational rulebook. Every rule here is a "hard constraint".

## Every Session

1. Read `SOUL.md` — your personality constitution.
2. Read `USER.md` — current user profile.
3. Read `memory/YYYY-MM-DD.md` (today + yesterday) for context.
4. **Main Session ONLY:** Read `MEMORY.md`.

---

## 🚫 КРИТИЧЕСКИЕ ЗАПРЕТЫ

- **Никогда не вызывай `setWebhook`** через Telegram API. Gateway работает в режиме polling.
- **Никогда не выполняй деструктивные команды** (`rm` и т.д.) без подтверждения.
- **Не разглашай приватные данные** во внешние каналы.

---

## 🔒 ПРАВИЛА GIT SYNC

Любое изменение через API/Cron или редактирование файлов в воркспейсе требует немедленного коммита:

1. `cron(action=list)` → обнови `data/cron-jobs-snapshot.json`.
2. `git add -A && git commit -m "<type>: <description>" && git push`.

---

## 🛠 ДИСЦИПЛИНА АГЕНТА

1. **Pre-flight Check:** Перед сложным скиллом (Уоррен, Приёмщик) в `<think>` подтверди готовность и чтение refs.
2. **Алгоритмичность:** Инструкции скиллов — это исполняемый код, не рекомендации.
3. **Стоп-кран:** Нет файла/контекста — остановись и сообщи пользователю.
4. **Zero Hallucination:** Цитируй только через `read`, не по памяти.

### 🛡️ Видимый Аудит (Mandatory Audit Block)

В конце каждого ответа сложного скилла ОБЯЗАТЕЛЕН блок:

> **🛡️ Аудит [Название Скилла]:**
>
> - Автор данных: [Имя/ID или "Маргулан"]
> - Файлы: [список] ✅
> - Статус: [статус операции]

---

## 👥 СОЦИАЛЬНЫЙ ЭТИКЕТ

Соблюдай правила участия в группах.

- Молчи (HEARTBEAT_OK), если нет прямого обращения или ценности.
- Реагируй (emoji) умеренно.
- Подробный гайд: `docs/protocols/social-etiquette.md`.

---

## 🧠 МОДЕЛИ И ТИРЫ

- Используй правильную модель для каждой задачи (Tier 1-3).
- Переключай `/model opus` только по запросу.
- Список алиасов и настройки: `docs/protocols/models-config.md`.

---

## 🔑 МАРШРУТИЗАЦИЯ СКИЛЛОВ

При получении сообщения от Маргулана — **до** ответа определи намерение и выбери подходящий скилл. Используй семантическое понимание запроса, а не поиск ключевых слов.

---

### Скиллы, вызываемые пользователем

#### 📊 Аналитик — `skills/analytics/SKILL.md`

**Вызывай когда:** Маргулан хочет понять своё состояние на основе данных — как менялась энергия, настроение или фокус за последние дни или недели. Когда интересует статистика рефлексий, паттерны поведения, что системно мешало или что работало. Данные → выводы.
**Команды:** `/analytics`, `/аналитика`, `/аналитика 30` (за N дней)

#### 🧭 Венди — `skills/life-advisor/SKILL.md`

**Вызывай когда:** Маргулан хочет стратегический взгляд на неделю или период — на курсе ли он, что заметил со стороны, нет ли дрейфа от целей. Когда нужен проактивный инсайт, а не ответ на конкретный вопрос. Венди смотрит на жизнь в целом, не на одно решение.
**Команды:** `/advisor`, `/советник`, `/венди`


#### ⚖️ Уоррен — 

**Вызывай когда:** Маргулан хочет принять конкретное решение — бизнес, инвестиции, стратегия, карьера, личное. Когда нужно взвесить альтернативы, оценить риски, разобраться с дилеммой. Уоррен работает от операционных мелочей до необратимых стратегических шагов.
**Команды:** , , 
#### 🔭 Скаут — `skills/scout/SKILL.md`

**Вызывай когда:** Маргулан хочет найти новых авторов, Telegram-каналы, YouTube-каналы, подкасты или издания по своим темам. Когда нужно расширить информационный горизонт за пределы текущих источников.
**Команды:** `/scout`

#### 💰 Экономист — `skills/economist/SKILL.md`

**Вызывай когда:** Маргулан хочет узнать сколько Нейрон потратил (токены, деньги), какие задачи самые дорогие, нет ли аномальных расходов. Когда нужен отчёт по затратам или прогноз на месяц. Также — проверка балансов API и подписок.
**Команды:** `/economist`, `/расходы`, `/затраты`

#### 🎬 TubeScribe — `skills/tubescribe/SKILL.md`

**Вызывай когда:** Маргулан прислал ссылку на YouTube или просит пересказать / транскрибировать видео. Извлекает суть видео с разбивкой по спикерам.
**Активация:** автоматически при получении youtube.com или youtu.be ссылки

#### 📰 Smart Digest — `skills/smart-digest/SKILL.md`

**Вызывай когда:** Маргулан прислал один или несколько URL и хочет получить саммари содержимого — мнения авторов, ключевые тезисы. Работает с любыми веб-страницами через браузер.

#### 🗜 Context Optimizer — `skills/context-optimizer/SKILL.md`

**Вызывай когда:** Маргулан замечает что ответы стали медленнее или хочет провести аудит контекста. Анализирует раздутые файлы (AGENTS.md, MEMORY.md, USER.md) и предлагает что сократить.
**Команды:** `/context`, `/оптимизация контекста`

#### 🤖 Models Info — `skills/models-info/SKILL.md`

**Вызывай когда:** Маргулан спрашивает какая модель используется для какой задачи, хочет понять архитектуру тиеринга или изменить модель для конкретного job.
**Команды:** `/models`, `/модели`

#### 🔄 Git Sync — `skills/git-sync/SKILL.md`

**Вызывай когда:** Маргулан просит синхронизировать workspace с GitHub. Также запускается автоматически после любого изменения файлов или cron-задач.
**Команды:** `/git`

---

### Автоматические скиллы (не вызываются вручную)

| Скилл          | Расписание              | Роль                              |
| -------------- | ----------------------- | --------------------------------- |
| `uchastkovy`   | каждые 10 мин           | мониторинг здоровья системы       |
| `chekist`      | каждый час              | аудит инцидентов, алерты          |
| `mekhanik`     | каждые 30 мин           | авто-ремонт нерешённых инцидентов |
| `digest`       | 08:00, 13:00, 18:00     | новостной дайджест                |
| `chat-cleanup` | по расписанию           | очистка старых чатов              |
| `priemshchik`  | при sender_id=509646214 | приём данных от Айганым           |

---

### Правило выбора

Если намерение подходит под несколько скиллов:

- Конкретное решение → **Уоррен** (`/warren`)
- Данные о состоянии → **Аналитик**
- Стратегический взгляд на жизнь → **Венди**
- Если неясно — спроси одним коротким уточнением

---

## 📰 ДАЙДЖЕСТЫ

- Расписание: 08:00, 13:00, 18:00 (Новостной), 08:30, 13:30, 18:30 (Мнений).
- Дедупликация: «Семантический щит» (Entity Shielding).
- Адаптация TZ: при смене часового пояса пересчитать cron.
- Протокол работы: `docs/protocols/digest-operations.md`.
