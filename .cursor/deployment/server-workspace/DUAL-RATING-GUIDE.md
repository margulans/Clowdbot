# 🎯 Dual Rating System - Руководство пользователя

## 🎯 Что это?

**Dual Rating System** - система двойной оценки новостей, которая отдельно оценивает:
- 📰 **Источники** (где взята новость)
- 👤 **Экспертов** (кто дал мнение)

Система использует **Multi-Armed Bandit** алгоритм для персонализации: 30% исследование новых источников/экспертов, 70% использование лучших.

## 🚀 Быстрый старт

### 1. Интеграция в OpenClaw агента

```javascript
// В начале файла агента
const { createDualRatingIntegration } = require('./openclaw-dual-rating-integration.js');

// Инициализация системы
const dualRatingSystem = createDualRatingIntegration({
    message: message,
    web_search: web_search, 
    memory_store: memory_store,
    memory_recall: memory_recall
});
```

### 2. Создание умного дайджеста

```javascript
// Команда /smart_digest
if (userMessage === '/smart_digest') {
    const result = await dualRatingSystem.createSmartDigest();
    console.log(`✅ Отправлено ${result.newsCount} персонализированных новостей`);
    return;
}
```

### 3. Обработка реакций пользователя

```javascript
// Webhook или polling для реакций
async function handleTelegramUpdate(update) {
    if (update.message_reaction) {
        const { message_id, chat, user, new_reaction } = update.message_reaction;
        
        // Обрабатываем только реакции от целевого пользователя
        if (user.id === 685668909 && new_reaction.length > 0) {
            const reaction = new_reaction[0].emoji;
            await dualRatingSystem.handleTelegramReaction(message_id, reaction, user.id);
        }
    }
}
```

## 📊 Система оценок

### Реакции и баллы
- 🔥 **Огонь** → +10 баллов
- 👍 **Лайк** → +5 баллов  
- 👎 **Дизлайк** → -3 балла
- 💩 **Мусор** → -5 баллов

### Статусы источников/экспертов
- ✅ **Proven** (7+ средний балл) - используются в 70% случаев
- 🔄 **Candidate** (от -2 до 7) - используются в 30% случаев
- ❌ **Rejected** (меньше -2) - исключаются из дайджестов

## 🎛️ Команды системы

### Основные команды

```javascript
// Создать умный дайджест
await dualRatingSystem.createSmartDigest();

// Получить отчет системы  
await dualRatingSystem.sendSystemReportToUser();

// Персонализированный поиск новостей
const news = await dualRatingSystem.searchPersonalizedNews('AI GPT', 'AI', 5);

// Очистка старых данных
await dualRatingSystem.cleanupOldData();
```

### Команды для пользователя

- `/smart_digest` - создать умный дайджест
- `/rating_report` - показать статистику системы
- `/cleanup_ratings` - очистить старые данные

## 🔧 Настройка категорий

### Поддерживаемые категории

```javascript
const categories = {
    'AI': {
        emoji: '🤖',
        defaultExperts: ['Андрей Карпатый', 'Сэм Альтман'],
        searchTerms: 'AI artificial intelligence OpenAI Anthropic'
    },
    'Robotics': {
        emoji: '🦾', 
        defaultExperts: ['Марк Райберт'],
        searchTerms: 'robotics humanoid robot Boston Dynamics'
    },
    'eVTOL': {
        emoji: '✈️',
        defaultExperts: ['JoeBen Bevirt'],
        searchTerms: 'eVTOL air taxi urban aviation'
    },
    'Tech': {
        emoji: '⚡',
        defaultExperts: ['Илон Маск', 'Тим Кук'],
        searchTerms: 'technology innovation startup'
    }
};
```

### Добавление новых категорий

```javascript
// Добавить источники
dualRatingSystem.dualRating.dualRating.addSource(
    'New Source Name',
    'https://source.url',
    'NewCategory'
);

// Добавить экспертов
dualRatingSystem.dualRating.dualRating.addExpert(
    'Expert Name',
    '@expert_handle', 
    'NewCategory'
);
```

## 📈 Алгоритм Multi-Armed Bandit

### Принцип работы

1. **70% Exploitation** - используем лучшие источники/экспертов
2. **30% Exploration** - пробуем новые или малооцененные

### Адаптация под пользователя

```javascript
// Система автоматически:
// 1. Отслеживает реакции пользователя 685668909
// 2. Обновляет рейтинги источников/экспертов
// 3. Адаптирует выбор для следующих дайджестов
// 4. Исключает плохо оцененный контент

const sourceSelection = dualRating.selectSourcesWithBandit(
    availableSources,
    10,     // количество источников
    0.3     // 30% exploration
);
```

## 📱 Формат новостей в канале

```
🤖 **OpenAI выпустил GPT-5**

Новая модель превосходит все предыдущие версии по производительности...

📰 **Источник:** OpenAI Blog
🔗 https://openai.com/blog/gpt5

💬 **Экспертное мнение:**
👤 Андрей Карпатый (@karpathy)  
"Это прорыв в области ИИ! Архитектурные улучшения впечатляют."

📊 **Оценка:**
🔥 Огонь (+10) | 👍 Лайк (+5) | 👎 Дизлайк (-3) | 💩 Мусор (-5)
💡 Реакции влияют на рейтинг источников и экспертов
```

## 📊 Отчеты системы

### Пример отчета

```
📊 **ОТЧЕТ DUAL RATING СИСТЕМЫ**

🗞️ **ИСТОЧНИКИ:**
✅ Proven: 12
🔄 Candidates: 8  
❌ Rejected: 2

🏆 **ТОП-5 ИСТОЧНИКОВ:**
1. OpenAI Blog (9.2⭐, 15 реакций)
2. Anthropic News (8.7⭐, 12 реакций)  
3. DeepMind Blog (8.1⭐, 9 реакций)
4. TechCrunch (6.4⭐, 22 реакций)
5. Habr.com (5.9⭐, 18 реакций)

👥 **ЭКСПЕРТЫ:**
✅ Proven: 8
🔄 Candidates: 12
❌ Rejected: 1

🏆 **ТОП-5 ЭКСПЕРТОВ:**
1. Андрей Карпатый (9.5⭐, 10 реакций)
2. Сэм Альтман (8.8⭐, 8 реакций)
3. Демис Хассабис (8.2⭐, 6 реакций)
4. Илон Маск (7.1⭐, 25 реакций)
5. Тим Кук (6.8⭐, 12 реакций)

🤖 **АЛГОРИТМ:** Multi-Armed Bandit (30/70)
👤 **TARGET USER:** 685668909
📊 **АКТИВНЫХ СООБЩЕНИЙ:** 127
```

## 🧹 Обслуживание системы

### Автоматическая очистка

```javascript
// В heartbeat функции агента
async function heartbeatMaintenance() {
    // Раз в день очищаем старые сообщения (>30 дней)
    const cleaned = await dualRatingSystem.cleanupOldData();
    
    // Раз в неделю генерируем отчет
    if (new Date().getDay() === 0) { // воскресенье
        await dualRatingSystem.sendSystemReportToUser();
    }
}
```

### Резервное копирование

```javascript
// Экспорт данных для бэкапа
const backupData = dualRatingSystem.dualRating.dualRating.exportData();
await fs.writeFile('backup-dual-rating.json', JSON.stringify(backupData, null, 2));

// Восстановление из бэкапа
const backupData = JSON.parse(await fs.readFile('backup-dual-rating.json'));
dualRatingSystem.dualRating.dualRating.importData(backupData);
```

## 🎯 Преимущества системы

### Для пользователя
- 📈 **Персонализация** - дайджесты адаптируются под ваши предпочтения
- 🎯 **Качество** - плохие источники автоматически исключаются  
- 🔍 **Обнаружение** - система находит новые качественные источники
- ⚡ **Скорость** - меньше шума, больше релевантного контента

### Для системы  
- 🤖 **Самообучение** - становится лучше с каждой реакцией
- 📊 **Аналитика** - детальная статистика предпочтений
- ⚖️ **Баланс** - оптимальное соотношение проверенного и нового
- 🔄 **Адаптивность** - быстро реагирует на изменения предпочтений

## 🚀 Roadmap

### Ближайшие улучшения
- [ ] Раздельные кнопки для источника и эксперта  
- [ ] Определение типа реакции по контексту
- [ ] Предиктивная система рекомендаций
- [ ] Интеграция с другими каналами (Discord, WhatsApp)

### Долгосрочные планы  
- [ ] Машинное обучение для лучшего понимания предпочтений
- [ ] Collaborative filtering между пользователями
- [ ] Автоматическая генерация экспертных мнений через ИИ
- [ ] Интеграция с социальными сетями для поиска экспертов

---

**✅ Dual Rating System готова к использованию!**

Система будет изучать ваши предпочтения и становиться точнее с каждым дайджестом. 🎯