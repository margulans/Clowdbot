# 🤖 Multi-Armed Bandit для Новостного Агрегатора

## 🎯 Описание

Адаптивная система выбора источников новостей на основе алгоритма Multi-Armed Bandit. Реализует принцип **"свинья у корыта"** — больше контента от лучших источников.

## 🔄 Принцип работы

### Баланс исследования и эксплуатации
- **70% Эксплуатация** — используем проверенные высокорейтинговые источники
- **30% Исследование** — тестируем новые источники для поиска лучших

### Система оценки источников
Реакции пользователя в канале @newsneiron:
- 🔥 **Огонь** = +10 баллов (отлично)
- 👍 **Лайк** = +5 баллов (нравится)  
- 👎 **Дизлайк** = -3 балла (не нравится)
- 💩 **Мусор** = -5 баллов (плохо)

### Статусы источников
- **candidate** — новый источник, тестируется
- **proven** — проверенный источник (средний рейтинг ≥7)
- **rejected** — отклоненный источник (средний рейтинг ≤-2)

## 📚 Файлы системы

```
multi-armed-bandit.js      # Основной алгоритм UCB
news-source-manager.js     # Интеграция и управление
digest-with-bandit.js      # Интеграция с OpenClaw
test-bandit.js            # Тестирование системы
data/source-bandit-state.json  # Сохраненное состояние
```

## 🚀 Использование

### Базовое использование

```javascript
const NewsSourceManager = require('./news-source-manager.js');

const manager = new NewsSourceManager();
await manager.initialize();

// Обработка реакции пользователя
await manager.handleUserReaction('OpenAI Blog', '🔥', 685668909);

// Выбор источников для дайджеста
const sources = ['OpenAI Blog', 'TechCrunch', 'Habr.com'];
const selection = await manager.selectSourcesForDigest(sources, 8);

console.log('Выбранные источники:', selection.sources);
```

### Интеграция с OpenClaw

```javascript
const { OpenClawDigestIntegration } = require('./digest-with-bandit.js');

const integration = new OpenClawDigestIntegration();

// Генерация умного дайджеста
const digest = await integration.generateSmartDigest(newsData);

// Обработка реакций из Telegram
const result = await integration.handleWebhookReaction(telegramUpdate);
```

## 📊 Мониторинг системы

### Получение статистики

```javascript
const stats = manager.getSourceStats();
console.log(`Проверенных источников: ${stats.byStatus.proven}`);
console.log(`Топ-5:`, stats.topSources.slice(0, 5));
```

### Рекомендации по оптимизации

```javascript
const recommendations = manager.getOptimizationRecommendations();
console.log(`Здоровье системы: ${recommendations.health.score}/100`);
console.log('Рекомендации:', recommendations.recommendations);
```

## 🎛️ Конфигурация

### Настройка баланса исследование/эксплуатация

```javascript
// В multi-armed-bandit.js
this.explorationRate = 0.30;  // 30% исследование
this.exploitationRate = 0.70; // 70% эксплуатация
```

### Настройка оценок реакций

```javascript
this.reactionScores = {
    '🔥': 10,  // Огонь - отлично
    '👍': 5,   // Лайк - нравится
    '👎': -3,  // Дизлайк - не нравится
    '💩': -5   // Мусор - плохо
};
```

### ID пользователя для фильтрации

```javascript
this.userId = 685668909; // Только реакции этого пользователя
```

## 🧪 Тестирование

```bash
# Запуск полного теста системы
node test-bandit.js

# Тест интеграции с OpenClaw  
node digest-with-bandit.js
```

## 📈 Алгоритм UCB (Upper Confidence Bound)

Система использует UCB для выбора источников:

```
UCB(source) = average_reward + confidence_interval

где:
- average_reward = общий рейтинг / количество показов
- confidence_interval = sqrt(2 * ln(total_selections) / source_selections)
```

Это позволяет балансировать между:
- **Exploitation** — выбор лучших известных источников
- **Exploration** — тестирование новых потенциально лучших источников

## 🔄 Рабочий процесс

1. **Инициализация** — загрузка состояния и добавление источников
2. **Генерация дайджеста** — выбор источников с помощью UCB
3. **Отправка в канал** — публикация новостей в @newsneiron  
4. **Сбор реакций** — мониторинг реакций пользователя
5. **Обновление рейтингов** — корректировка на основе обратной связи
6. **Сохранение состояния** — персистентность данных

## 🎯 Преимущества системы

- **Персонализация** — адаптация под предпочтения пользователя
- **Автоулучшение** — система становится лучше со временем
- **Баланс** — оптимальное соотношение качества и разнообразия
- **Прозрачность** — понятная логика выбора источников
- **Масштабируемость** — легко добавлять новые источники

## 📊 Метрики здоровья системы

- **Отлично (80-100)** — достаточно проверенных источников, хороший баланс
- **Хорошо (60-79)** — система работает, есть область для улучшений  
- **Удовлетворительно (40-59)** — требуются новые источники
- **Нужно улучшение (<40)** — критические проблемы с источниками

## 🔧 Техническая реализация

- **Node.js** — основная платформа
- **JSON файлы** — персистентное хранение состояния
- **UCB алгоритм** — для оптимального выбора источников
- **Telegram API** — для сбора реакций пользователей
- **OpenClaw интеграция** — бесшовная работа с существующей системой

## 🐷 Философия "Свинья у корыта"

> Дай свинье больше того корма, который она ест с удовольствием, и меньше того, который игнорирует.

Применительно к новостям:
- Больше источников с реакциями 🔥 и 👍
- Меньше источников с реакциями 👎 и 💩
- Постоянное тестирование новых источников
- Автоматическая оптимизация под пользователя

---

**✅ Multi-Armed Bandit система готова к работе!**

Система будет автоматически улучшаться с каждой твоей реакцией, предлагая всё более релевантный и качественный контент.