---
name: scout
description: Скаут — еженедельный дип-ресёрч интернета, соцсетей, YouTube и Telegram-каналов для поиска новых авторов, источников и тем для дайджестов
command-description: Запустить разведку новых источников и авторов
user-invocable: yes
metadata: { "openclaw": { "emoji": "🔭", "requires": { "config": ["brave.apiKey"] } } }
---

# Scout — Разведчик

Изолированный субагент для глубокого исследования интернета, соцсетей, YouTube и Telegram-каналов.

**Цель:** найти новых авторов, источники, каналы и темы для дайджестов — исходя из явных и неявных интересов Маргулана.

## Запуск

- **Автоматически (ресёрч):** каждое воскресенье в 02:00 Алматы — режим `research`
- **Автоматически (отчёт):** каждое воскресенье в 12:00 Алматы — режим `report`
- **Вручную:** `/scout` — запускает полный цикл (ресёрч + отчёт сразу)

---

## РЕЖИМ 1: РЕСЁРЧ (`research`)

Запускается ночью (02:00 Алматы). Собирает данные, сохраняет в файл. **Уведомление НЕ отправляет.**

### Шаг 1: Контекст — профиль интересов Маргулана

Прочитай последовательно:

1. `USER.md` — явные интересы, новостные приоритеты
2. `MEMORY.md` — долгосрочный контекст, обязательные авторы мнений
3. `memory/YYYY-MM-DD.md` — последние 7 дней (о чём говорили, какие темы поднимались)
4. `data/dual-rating-data.json` — что получало 🔥 (сильные сигналы интересов)
5. `data/source-status-tracking.json` — что уже в ротации (чтобы не дублировать)

Из прочитанного составь профиль:

- **Явные интересы:** ИИ, Вайбкодинг, Робототехника, eVTOL, Технологии, Бизнес, Инвестиции
- **Неявные интересы:** темы из 🔥-реакций, вопросы из рефлексий, темы разговоров
- **Фреймворки:** антихрупкость, Barbell, Lindy, шкура на кону — ищи авторов в этой парадигме
- **Жизненный контекст:** 59 лет — релевантны темы longevity, biohacking, качество жизни
- **Особый трек:** везение и удача — научные исследования, закономерное везение

### Шаг 2: Дип-ресёрч по 6 трекам

Для каждого трека выполни указанные запросы. Используй `web_search` с провайдером `perplexity` для синтезирующих запросов и `brave` для конкретных поисков.

---

#### Трек A: Новые авторы и эксперты (Twitter/X, Substack, блоги)

По каждой категории из профиля:

```
web_search(provider=perplexity): "who are the most insightful emerging voices in [AI/robotics/eVTOL/investing] in 2026?"
web_search(provider=brave): "top AI researchers twitter 2026 new voices"
web_search(provider=brave): "новые авторы про искусственный интеллект блог субстак 2026"
web_search(provider=brave): "best substack newsletters AI robotics technology 2026"
```

Для каждого кандидата: если есть URL — сделай `web_fetch` страницы профиля/блога для проверки активности.

---

#### Трек B: Telegram-каналы

```
web_search(provider=brave): "лучшие Telegram каналы искусственный интеллект 2026"
web_search(provider=brave): "топ Telegram каналы технологии вайбкодинг 2026"
web_search(provider=brave): "best telegram channels AI robotics evtol 2026"
web_search(provider=brave): "telegram channel luck science psychology 2026"
```

Для каждого найденного канала — проверь публичный веб-просмотр:

```
web_fetch("https://t.me/s/<channel_name>")
```

Извлеки: название, тематику, последние публикации, частоту постов.

---

#### Трек C: YouTube-каналы

```
web_search(provider=brave): "best youtube channels artificial intelligence 2026 new"
web_search(provider=brave): "youtube каналы вайбкодинг разработка ИИ 2026"
web_search(provider=brave): "youtube channels robotics evtol emerging 2026"
web_search(provider=brave): "youtube channels science of luck probability 2026"
```

Для топ-кандидатов — проверь страницу канала:

```
web_fetch("https://www.youtube.com/@<channel>/about")
```

---

#### Трек D: Новые издания, рассылки, медиа

```
web_search(provider=perplexity): "best new newsletters technology AI 2026 worth subscribing"
web_search(provider=brave): "new tech media publications launched 2026"
web_search(provider=brave): "новые медиа издания технологии ИИ 2026"
```

---

#### Трек E: Новые темы — вне текущего радара

```
web_search(provider=perplexity): "what emerging technology trends in 2026 are not yet mainstream?"
web_search(provider=perplexity): "технологические тренды 2026 о которых мало говорят"
```

Ищи темы, которых **нет** в текущих 7 категориях дайджеста, но потенциально близки интересам Маргулана.

---

#### Трек F: Везение и удача

```
web_search(provider=perplexity): "science of luck research 2026 new studies findings"
web_search(provider=brave): "закономерное везение научные исследования психология 2026"
web_search(provider=brave): "luck vs skill studies scientists authors 2026"
web_search(provider=brave): "Richard Wiseman luck new research 2026"
web_search(provider=brave): "systematic luck patterns behavioral economics 2026"
```

Ищи: новые исследования, авторов, данные, мнения экспертов о природе везения и его закономерностях.

---

### Шаг 3: Фильтрация и оценка

Для каждого кандидата из всех треков:

**Проверь дедупликацию:**

- Уже есть в `data/source-status-tracking.json`? → пропусти
- Уже есть в `data/scout-discoveries.json` с `added_to_digest: true`? → пропусти
- Уже есть в `data/scout-discoveries.json` со статусом `rejected`? → пропусти

**Оцени релевантность (1-10) по критериям:**

- Соответствие явным интересам Маргулана (+3)
- Соответствие неявным интересам из памяти (+2)
- Свежесть: активен в 2026, публикации есть (+2)
- Глубина/качество контента (не поверхностно) (+2)
- Уникальность точки зрения (нет в текущей ротации) (+1)

**Проходной порог: ≥ 7 баллов.** Кандидаты с ≤ 6 — отбрасываются.

### Шаг 4: Сохранение в scout-discoveries.json

Прочитай текущий `data/scout-discoveries.json`. Добавь новых кандидатов (не перезаписывай старые записи). Для каждого:

```json
{
  "id": "scout_YYYYMMDD_NNN",
  "type": "author|channel|source|topic",
  "name": "Имя или название",
  "url": "https://...",
  "platform": "twitter|substack|youtube|telegram|blog|newsletter",
  "category": "ИИ|Вайбкодинг|Робототехника|eVTOL|Технологии|Бизнес|Инвестиции|Везение|Новая",
  "relevance_score": 8,
  "summary": "Краткое описание: кто это / что это / о чём",
  "reason": "Почему релевантно Маргулану — конкретная связь с его интересами",
  "status": "pending",
  "discovered_at": "YYYY-MM-DD",
  "added_to_digest": false,
  "added_to_digest_at": null,
  "added_to_digest_type": null
}
```

Обнови `"last_run"` на текущее время.

### Шаг 5: Git-коммит (только ресёрч-данные)

```bash
git add data/scout-discoveries.json
git commit -m "scout: research run YYYY-MM-DD — N new candidates"
git push
```

---

## РЕЖИМ 2: ОТЧЁТ (`report`)

Запускается днём (12:00 Алматы). Читает сохранённые данные, формирует и отправляет отчёт.

### Шаг 1: Читай scout-discoveries.json

Загрузи `data/scout-discoveries.json`. Отбери записи:

- `status: "pending"` И `added_to_digest: false`
- Созданные за последние 7 дней

Если новых находок нет → отправь в личку: `🔭 Scout: на этой неделе новых кандидатов не найдено.` → завершить.

### Шаг 2: Сгруппируй по типу

- 👤 Авторы и эксперты
- 📱 Telegram-каналы
- ▶️ YouTube-каналы
- 📰 Издания и рассылки
- 🔭 Новые темы
- 🍀 Везение и удача

Внутри каждой группы — сортируй по `relevance_score` (убывание).

### Шаг 3: Сформируй SCOUT REPORT

```
🔭 SCOUT REPORT | ДД.ММ.ГГГГ

Нашёл N новых кандидатов для дайджестов:

👤 Авторы (X):
• [Имя] (@handle) — [1 строка: кто + почему релевантно]
• ...

📱 Telegram (Y):
• @channel — [1 строка: тематика + почему]
• ...

▶️ YouTube (Z):
• [Название канала] — [1 строка: тематика + почему]
• ...

📰 Издания / рассылки (W):
• [Название] — [1 строка]
• ...

🔭 Новые темы (V):
• [Тема] — [1 строка: что это + потенциальная ценность]
• ...

🍀 Везение и удача (U):
• [Автор/источник/исследование] — [1 строка]
• ...

──────────────────
📁 Полные данные: data/scout-discoveries.json
💬 Чтобы добавить: «Нейрон, добавь [название] в дайджест [новостей/мнений]»
```

### Шаг 4: Отправь в личку

```json
{
  "channel": "telegram",
  "action": "sendMessage",
  "to": "tg:685668909",
  "content": "<сформированный отчёт>"
}
```

**Важно:** Только в личку (685668909). НЕ в каналы @newsneiron или @opnioneiron.

### Шаг 5: Git-коммит (финальный)

```bash
git add data/scout-discoveries.json
git commit -m "scout: weekly report sent YYYY-MM-DD — N candidates"
git push
```

---

## Обработка команды «Добавь в дайджест»

Когда Маргулан говорит: _«Нейрон, добавь @channel в дайджест новостей»_:

1. Найди запись в `data/scout-discoveries.json` по имени/url
2. Добавь источник/автора/тему в `news-config.md` (или соответствующий конфиг дайджеста)
3. Обнови запись в `scout-discoveries.json`:
   ```json
   {
     "status": "accepted",
     "added_to_digest": true,
     "added_to_digest_at": "YYYY-MM-DD",
     "added_to_digest_type": "news|opinions"
   }
   ```
4. Git-коммит изменений
5. Подтверди Маргулану: `✅ @channel добавлен в дайджест [тип]. Первые результаты — на следующем выпуске.`

---

## Обработка ошибок

- `web_fetch` недоступен → пропусти проверку, используй только web_search данные
- Telegram канал не имеет публичного просмотра → отметь `"platform": "telegram"`, добавь с пометкой `"summary": "[не проверено — нет публичного превью]"`
- `data/scout-discoveries.json` не существует → создай пустой файл со структурой `{"last_run": null, "discoveries": []}`
- Нет новых кандидатов после фильтрации → в отчёте сообщить об этом, не отправлять пустой отчёт

---

## Аудит (обязателен в конце каждого запуска)

> **🛡️ Аудит Scout:**
>
> - Режим: [research|report]
> - Треки: [A/B/C/D/E/F — выполнены]
> - Кандидатов найдено: N | Прошли фильтр: M
> - Файл: data/scout-discoveries.json ✅
> - Git: [commit hash или "нет изменений"]
> - Статус: ✅ Завершён
